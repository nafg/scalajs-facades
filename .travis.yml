jdk:
  - openjdk8
language: scala
before_install:
  - nvm install 12
  - curl -o- -L https://yarnpkg.com/install.sh | bash
script: "sbt +test"
deploy:
  provider: script
  script: "sbt +publish"
  on:
    tags: true
